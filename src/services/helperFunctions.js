// Uncatagorized helper functions for various uses 

export const helperFunctions = {
    parseErrorLogin     
};

// Parses error messages from login attempts
function parseErrorLogin (error) {
    console.log(error);

}

